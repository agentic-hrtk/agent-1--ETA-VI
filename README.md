Here is the updated content of the README.md file with the typo fixed:

# agent-1--ETA-VI

## Setup

To install the project, run the following command:

```bash
pip install -r requirements.txt
```

...